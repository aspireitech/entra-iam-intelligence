# Microsoft Entra application (SPA) client ID
# Register the app as a multitenant SPA and add this exact redirect URI:
# http://localhost:5173
VITE_ENTRA_CLIENT_ID=

# Optional. Defaults to the Microsoft Entra organizations endpoint.
VITE_ENTRA_AUTHORITY=https://login.microsoftonline.com/organizations
